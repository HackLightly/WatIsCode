#include <stdio> 
#include <regex>
#include <string>
#include <fstream>

using std::cout;
using std::cin;
using std::ifstream;
using std::ofstream;
using std::endl;
using std::string;

int main (int argc, char* argv[]) {
	string line;
	while (getline(cin, line)) {
		if (line == "") continue;
		
		
	}
}
